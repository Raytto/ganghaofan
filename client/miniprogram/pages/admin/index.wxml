<navigation-bar title="管理" back="{{false}}" color="#C9D1D9" background="#1B1B1B"></navigation-bar>
<view class="page">
  <view class="section">管理员功能区</view>
  <block wx:if="{{!canAdmin}}">
    <view class="hint">当前账号无管理权限</view>
  </block>
  <view class="row">
    <text>管理视图模式</text>
    <switch checked="{{adminViewEnabled}}" bindchange="onToggleAdminView"></switch>
  </view>
  <view class="hint">开启后首页按管理员模式显示；关闭则按普通用户模式显示。</view>
</view>
